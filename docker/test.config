%% CT configuration for distributed integration tests
[
    %% Mycelium configuration for test nodes
    {mycelium, [
        {active_size, 5},
        {passive_size, 30},
        {shuffle_period, 5000},  %% Faster shuffle for tests
        {max_fail_count, 3},
        {base_backoff_ms, 500},
        {passive_max_age_ms, 60000},
        {churn_window_ms, 10000}
    ]}
].
