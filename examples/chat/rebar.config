{erl_opts, [debug_info]}.

{deps, [
    %% _checkouts/mycelium symlink overrides this for local development
    {mycelium, {git, "https://github.com/benoitc/mycelium.git", {branch, "main"}}}
]}.

{shell, [
    {apps, [chat]},
    {config, "config/sys.config"}
]}.

{relx, [
    {release, {chat, "0.1.0"}, [
        chat,
        mycelium,
        runtime_tools
    ]},
    {dev_mode, false},
    {include_erts, true},
    {extended_start_script, true},
    {sys_config, "config/sys.config"},
    {vm_args, "config/vm.args"}
]}.

{profiles, [
    {prod, [
        {relx, [
            {dev_mode, false},
            {include_erts, true}
        ]}
    ]}
]}.
