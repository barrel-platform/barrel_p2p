%% -*- erlang -*-
%%
%% sys.config for the chat example. Only barrel_p2p envs are needed;
%% barrel_p2p_dist projects the underlying quic_dist wiring at listen
%% time.
[
 {barrel_p2p, [
    {active_size, 3},
    {passive_size, 10},
    {shuffle_period, 5000},
    {listen_port, 9100},
    {contact_nodes, []},
    {dist_cookie, chat},

    {auth_enabled, true},
    {auth_key_dir, "data/keys"},
    {auth_trust_mode, tofu},

    {quic_cert_dir, "data/quic"},
    {discovery_dir, "data/discovery"},
    {discovery_backends,
     [barrel_p2p_discovery_static,
      barrel_p2p_discovery_file,
      barrel_p2p_discovery_dns]}
 ]},
 {kernel, [
    {logger_level, info},
    {logger, [
        {handler, default, logger_std_h, #{
            formatter => {logger_formatter, #{
                template => [time, " ", level, " ", msg, "\n"]
            }}
        }}
    ]}
 ]}
].
